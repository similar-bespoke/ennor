# Ennor PDF-to-Markdown conversion pipeline
# Install: pip3 install -r requirements.txt

# PDF text extraction
PyMuPDF>=1.23.0

# OCR for scanned PDFs (requires Tesseract + Poppler system packages)
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0

# YAML frontmatter (stdlib, but listed for clarity)
# pyyaml not actually needed — we write YAML by hand

# System dependencies (install separately):
#   brew install tesseract poppler    # macOS
#   apt install tesseract-ocr poppler-utils  # Debian/Ubuntu
